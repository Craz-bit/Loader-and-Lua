local player = game:GetService("Players").LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local coreGui = game:GetService("CoreGui")
local replicatedStorage = game:GetService("ReplicatedStorage")
local remote = replicatedStorage.Remotes.CommF_

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ModernFruitSpawner"
screenGui.Parent = coreGui
screenGui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 260, 0, 160)
mainFrame.Position = UDim2.new(0.5, -130, 0.4, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 31, 47)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = mainFrame

local titleBar = Instance.new("TextLabel")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundTransparency = 1
titleBar.Text = "✨ TNT Spawner" -- Pinalitan na ang title dito
titleBar.TextColor3 = Color3.fromRGB(255, 230, 150)
titleBar.TextSize = 18
titleBar.Font = Enum.Font.GothamBold
titleBar.Parent = mainFrame

local inputArea = Instance.new("TextBox")
inputArea.Size = UDim2.new(0.85, 0, 0, 35)
inputArea.Position = UDim2.new(0.075, 0, 0.3, 0)
inputArea.BackgroundColor3 = Color3.fromRGB(40, 41, 58)
inputArea.PlaceholderText = "Enter Fruit Name..."
inputArea.Text = ""
inputArea.TextColor3 = Color3.new(1, 1, 1)
inputArea.Font = Enum.Font.Gotham
inputArea.TextSize = 14
inputArea.Parent = mainFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 8)
inputCorner.Parent = inputArea

local spawnBtn = Instance.new("TextButton")
spawnBtn.Size = UDim2.new(0.85, 0, 0, 40)
spawnBtn.Position = UDim2.new(0.075, 0, 0.6, 0)
spawnBtn.BackgroundColor3 = Color3.fromRGB(55, 56, 78)
spawnBtn.Text = "Fruit Spawn"
spawnBtn.TextColor3 = Color3.new(1, 1, 1)
spawnBtn.Font = Enum.Font.GothamBold
spawnBtn.TextSize = 14
spawnBtn.AutoButtonColor = true
spawnBtn.Parent = mainFrame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = spawnBtn

-- Credit text (bagong idinagdag)
local creditLabel = Instance.new("TextLabel")
creditLabel.Size = UDim2.new(0.85, 0, 0, 20)
creditLabel.Position = UDim2.new(0.075, 0, 0.88, 0)
creditLabel.BackgroundTransparency = 1
creditLabel.Text = "Made by Mochii Scripts"
creditLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
creditLabel.TextSize = 11
creditLabel.Font = Enum.Font.Gotham
creditLabel.TextTransparency = 0.3
creditLabel.Parent = mainFrame

-- (Rest ng code mo remains the same)
local lastFruitName = ""
local statusMode = "none" 
local KEYWORD = "fruit added to"

local function watchObject(obj)
    if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
        local function checkText()
            local text = string.lower(obj.Text)
            if string.find(text, KEYWORD) or string.find(text, "spawned successfully") or string.find(text, "not avalible") then
                if statusMode == "error" then
                    obj.Text = "Dragon (west) Is Currently not Avalible."
                    obj.TextColor3 = Color3.fromRGB(255, 80, 80)
                elseif statusMode == "spawn" then
                    obj.Text = lastFruitName .. " Fruit spawned successfully!"
                    obj.TextColor3 = Color3.fromRGB(144, 238, 144)
                end
            end
        end
        checkText()
        obj:GetPropertyChangedSignal("Text"):Connect(checkText)
    end
end

for _, obj in ipairs(playerGui:GetDescendants()) do watchObject(obj) end
playerGui.DescendantAdded:Connect(watchObject)

spawnBtn.MouseButton1Click:Connect(function()
    local fruit = inputArea.Text
    if fruit == "" then return end

    if fruit == "DragonEast" then
        statusMode = "spawn"
        lastFruitName = "Dragon"
        loadstring(game:HttpGet("https://raw.githubusercontent.com/MoziIOnTop/pro/refs/heads/main/DragonEastSpawner.lua"))()
    elseif fruit == "DragonWest" then
        statusMode = "error"
    else
        statusMode = "spawn"
        lastFruitName = fruit
        remote:InvokeServer("LoadFruit", fruit .. "-" .. fruit)
    end
end)
